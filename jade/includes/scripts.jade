//Place references to your scripts here
script(src="js/jquery.js")
script(src="js/bootstrap.min.js")
script(src="js/validator.min.js")
script(src="js/egress-validators.js")
script(src="js/googleanalytics.js")
script.
    if(window.location.href !== "http://today-was.herokuapp.com/" && window.location.href !== "http://todaywas.herokuapp.com/") {
        var msg = "<div class=\"container\"><div class=\"alert alert-danger text-center\"><strong>You are on a development server.</strong></div></div>";
        //var html = document.getElementsByClassName("container")[document.getElementsByClassName("container").length-1].innerHTML;
        //document.getElementsByClassName("container")[document.getElementsByClassName("container").length-1].innerHTML = msg+html;
        $(".container").eq(0).next().html(msg + $(".container").eq(0).next().html());
    }
    
    function htmlDecode(input){
        e = document.createElement("div");
        e.innerHTML = input;
        return e.childNodes.length === 0 ? "" : e.childNodes[0].nodeValue;
    }
    
    $(".btn").button();
    $("#journalContentHidden").removeClass("hidden");